package basics

import "fmt"

func main() {
	var input string

	for {
		fmt.Println("New input")
		value, err := fmt.Scan(&input)

		if err == nil {
			fmt.Println("Char as int: ", value)
			fmt.Println("raw input: ", input)
		} else {
			fmt.Println("Error: ", err)
		}
	}
}
